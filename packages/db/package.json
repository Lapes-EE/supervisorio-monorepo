{
  "name": "@repo/db",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "dotenv -e ../../.env.test.local vitest run"
  },
  "dependencies": {
    "@repo/env": "workspace:*",
    "@repo/telemetry": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "@repo/tsconfig": "workspace:*",
    "@types/node": "^24.0.15",
    "dotenv-cli": "^8.0.0",
    "vitest": "^3.0.5"
  }
}
